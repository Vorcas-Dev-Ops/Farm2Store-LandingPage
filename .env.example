# Environment Configuration

# Application Settings
VITE_APP_TITLE=Farm2Store

# Backend API URL (use relative path for production, full URL for dev)
VITE_API_URL=/api/quote

# Email Configuration (only needed in Backend/.env, not in frontend)
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

